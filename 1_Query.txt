Create database QlyBanHang
go
use QlyBanHang

GO

Create table tblCategory(
Id nvarchar(50) not null primary key,
NameCategory nvarchar(255),
Status bit
)
go
create table tblProduct(
Id nvarchar(50) not null primary key,
IdCategory nvarchar(50),
NameProduct nvarchar(255),
Color nvarchar(255),
Size nvarchar(100),
Price float,
Quantity int,
Description nvarchar(2000),
createdDate datetime
)
go
create table tblBill(
Id nvarchar(50) not null primary key,
Biller nvarchar(255),
NameCustomer nvarchar(255),
TotalMoney float,
CreateDate datetime
)
go
create table tblInvoiceDetails(
Id nvarchar(50) not null primary key,
IdBill nvarchar(50),
IdProduct nvarchar(50),
Quantity int,
TotalMoney float,
WarraytyPeriod datetime
)
go
create table tblReceiptSlip(
Id nvarchar(50) not null primary key,
NameCustomer nvarchar(255),
Address nvarchar(255),
ReceivingTime datetime,
Receiver nvarchar(255),
PayTime nvarchar(255)
)
go
create table tblEquipmentNeedWarrayty(
Id nvarchar(50) not null primary key,
NameProduct nvarchar(255),
Status nvarchar(255),
Description nvarchar(255)
)
go
create table tblRepairLog(
Id nvarchar(50) not null primary key,
NameProduct nvarchar(255),
Solution nvarchar(255),
Repairer nvarchar(255),
FixDate datetime,
RepairTime datetime
)
go
alter table tblProduct ADD CONSTRAINT FK_Category
FOREIGN KEY (IdCategory) REFERENCES tblCategory(Id);

go
alter table tblInvoiceDetails ADD CONSTRAINT FK_Bill
FOREIGN KEY (IdBill) REFERENCES tblBill(Id);

go
alter table tblInvoiceDetails ADD CONSTRAINT FK_Product
FOREIGN KEY (IdProduct) REFERENCES tblBill(Id);

